<footer class="py-16 px-6 bg-slate-950 border-t border-white/5">
  <div class="max-w-7xl mx-auto">
    <div class="grid md:grid-cols-4 gap-10">
      <div class="space-y-4">
        <p class="text-xl font-black text-white">Retain<span class="text-indigo-400">OS</span></p>
        <p class="text-sm text-slate-600 leading-relaxed max-w-xs">
          The operating system for modern dental practices. Launch your branded patient app in 20 minutes.
        </p>
      </div>

      <div>
        <p class="text-sm font-bold text-white mb-4 uppercase tracking-widest">Product</p>
        <ul class="space-y-3">
          {[{ href: '#platform', label: 'Platform' }, { href: '#how-it-works', label: 'How It Works' }, { href: '#faq', label: 'FAQ' }, { href: '#waitlist', label: 'Get Started' }, { href: '/blog', label: 'Blog' }, { href: '/blog/dental-loyalty-app-guide', label: 'Loyalty App Guide' }].map(item => (
            <li><a href={item.href} class="text-sm text-slate-500 hover:text-white transition-colors">{item.label}</a></li>
          ))}
        </ul>
      </div>

      <div>
        <p class="text-sm font-bold text-white mb-4 uppercase tracking-widest">Company</p>
        <ul class="space-y-3">
          {[{ label: 'About' }, { label: 'Blog' }, { label: 'Careers' }, { label: 'Contact' }].map(item => (
            <li><span class="text-sm text-slate-500">{item.label}</span></li>
          ))}
        </ul>
      </div>

      <div>
        <p class="text-sm font-bold text-white mb-4 uppercase tracking-widest">Legal</p>
        <ul class="space-y-3">
          {[{ label: 'Privacy Policy' }, { label: 'Terms of Service' }, { label: 'HIPAA Compliance' }, { label: 'Security' }].map(item => (
            <li><span class="text-sm text-slate-500">{item.label}</span></li>
          ))}
        </ul>
      </div>
    </div>

    <div class="mt-12 pt-8 border-t border-white/5 flex flex-col md:flex-row items-center justify-between gap-4">
      <p class="text-xs text-slate-700">&copy; {new Date().getFullYear()} RetainOS. All rights reserved.</p>
      <div class="flex items-center gap-4">
        <span class="text-xs text-slate-700">HIPAA Compliant</span>
        <span class="w-1 h-1 rounded-full bg-slate-700"></span>
        <span class="text-xs text-slate-700">SOC 2 Type II</span>
        <span class="w-1 h-1 rounded-full bg-slate-700"></span>
        <span class="text-xs text-slate-700">AES-256 Encrypted</span>
      </div>
    </div>
  </div>
</footer>