<section class="relative py-24 px-6 bg-ink-950 overflow-hidden">
  <div class="absolute inset-0 bg-gradient-to-b from-ink-950/40 via-ink-950/60 to-ink-950/40 pointer-events-none"></div>

  <div class="max-w-3xl mx-auto text-center relative z-10">
    <h2 class="text-[clamp(2rem,4vw,3.5rem)] font-black text-white leading-[1.05] tracking-tighter text-balance">
      Ready to grow your practice?
    </h2>
    <p class="text-white/70 text-[clamp(1rem,1.5vw,1.125rem)] mt-4 max-w-2xl mx-auto">
      Discover how RetainOS can deliver measurable ROI in just weeks. See why dental practices are switching to automated retention.
    </p>
    <div class="flex flex-col sm:flex-row justify-center gap-4 mt-10">
      <button onclick="window.dispatchEvent(new Event('open-demo-form'))" class="inline-flex items-center gap-2 rounded-[32px] px-8 py-4 bg-white text-dark-blue font-semibold text-lg hover:opacity-90 transition-all duration-200 shadow-sm">
        Book a demo
      </button>
      <a href="/pricing" class="inline-flex items-center gap-2 rounded-[32px] px-8 py-4 border border-white/20 text-white font-semibold text-lg hover:bg-white/10 transition-all duration-200">
        View pricing
      </a>
    </div>
    <div class="flex flex-wrap items-center justify-center gap-x-6 gap-y-2 mt-8 text-xs text-white/50">
      <span class="flex items-center gap-1.5">
        <svg class="w-3 h-3 text-emerald-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg>
        HIPAA compliant
      </span>
      <span class="flex items-center gap-1.5">
        <svg class="w-3 h-3 text-emerald-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg>
        SOC 2 certified
      </span>
      <span class="flex items-center gap-1.5">
        <svg class="w-3 h-3 text-emerald-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg>
        No contract. Cancel anytime.
      </span>
    </div>
  </div>
</section>
