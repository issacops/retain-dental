<section class="relative py-24 px-6 bg-ink-900 overflow-hidden border-t border-b border-white/5" id="demo-callout">
  <div class="max-w-5xl mx-auto text-center relative z-10">
    <p class="text-xs font-semibold text-violet-400 uppercase tracking-[0.15em] mb-4">Hear how retention gets handled</p>
    <h2 class="text-[clamp(1.75rem,3.5vw,2.75rem)] font-black text-white leading-[1.05] tracking-tighter text-balance">
      Before your team even picks up the phone
    </h2>
    <p class="text-white/70 text-[clamp(1rem,1.5vw,1.125rem)] max-w-2xl mx-auto mt-4">
      RetainOS automatically detects missed appointments, sends recall sequences, and re-engages patients — all without a single manual call from your front desk.
    </p>
    <button onclick="window.dispatchEvent(new Event('open-demo-form'))" class="inline-flex items-center gap-2 rounded-[25px] px-8 py-4 bg-violet-600 text-white font-semibold text-lg hover:opacity-90 transition-all duration-200 shadow-layer mt-8">
      See it in action
      <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="5 3 19 12 5 21 5 3"/></svg>
    </button>
  </div>
</section>
