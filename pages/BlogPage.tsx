import React from 'react';
import { Navigate } from 'react-router-dom';

export const BlogIndexPage: React.FC = () => {
    return <Navigate to="https://retaindental.com/blog" replace />;
};

export const BlogPostPage: React.FC = () => {
    return <Navigate to="https://retaindental.com/blog" replace />;
};
